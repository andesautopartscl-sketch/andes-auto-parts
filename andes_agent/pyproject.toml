[project]
name = "andes-agent"
version = "0.11.0"
description = "Andes Auto Parts Agent Gateway (isolated from ERP core)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["andes_agent*"]
