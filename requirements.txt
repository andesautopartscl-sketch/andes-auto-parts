Flask
Flask-SQLAlchemy
SQLAlchemy
pandas
openpyxl
reportlab
python-dotenv
python-barcode
qrcode
Pillow
cloudinary==1.44.2
requests
APScheduler
google-api-python-client==2.111.0
google-auth==2.27.0
google-auth-httplib2
google-auth-oauthlib
google-cloud-vision==3.7.0
pymupdf==1.24.0
Flask-Limiter
rapidfuzz>=3.0
tzdata>=2024.1

# Optional (prod): only needed if deploying behind a WSGI server
gunicorn

# Assistant NL planner (ERP only — never install into andes_agent)
openai>=1.40.0

# Assistant Secret Vault (FASE 10.3) --- AES-256-GCM via AEAD.
# El piso no es una intuicion: pyOpenSSL --- que ya entra con las
# bibliotecas de Google Drive --- exige cryptography>=46.0.0, asi que el
# proyecto YA dependia de esto de forma transitiva. Declararlo solo hace
# explicito lo que una instalacion limpia ya resuelve, y evita que una
# reorganizacion de los extras de Google se lleve por delante el vault.
cryptography>=46.0
