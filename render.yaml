services:
  - type: web
    name: andes-auto-parts
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:app
    healthCheckPath: /login
    disk:
      name: andes-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
      # SQLite + varios workers de Gunicorn provoca "database is locked" y 500 intermitentes.
      - key: WEB_CONCURRENCY
        value: "1"

