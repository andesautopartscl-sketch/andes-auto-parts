(function () { "use strict"; var SPLASH_MIN_MS = 480; var SPLASH_HARD_MAX_MS = 2200; function setProgress(splash, value) { var bar = document.getElementById("mobile-splash-progress"); var progress = splash && splash.querySelector(".mobile-splash__progress"); var pct = Math.max(0, Math.min(100, Math.round(value))); if (bar) bar.style.width = pct + "%"; if (progress) progress.setAttribute("aria-valuenow", String(pct)); } function forceUnlockUi(splash) { document.body.classList.remove("mobile-app--splash"); if (!splash) return; splash.classList.add("mobile-splash--hide"); splash.classList.add("mobile-splash--gone"); splash.setAttribute("aria-hidden", "true"); splash.style.pointerEvents = "none"; try { if (splash.parentNode) splash.parentNode.removeChild(splash); } catch (_e) {} } function hideSplash(splash) { if (!splash || splash.dataset.andesSplashDone === "1") return; splash.dataset.andesSplashDone = "1"; setProgress(splash, 100); splash.classList.add("mobile-splash--ready"); splash.style.pointerEvents = "none"; document.body.classList.remove("mobile-app--splash"); splash.classList.add("mobile-splash--hide"); window.setTimeout(function () { forceUnlockUi(splash); }, 280); } document.addEventListener("DOMContentLoaded", function () { var splash = document.getElementById("mobile-splash"); if (!splash) return; document.body.classList.add("mobile-app--splash"); var started = Date.now(); var finished = false; var current = 8; setProgress(splash, current); function bumpProgress(target) { current = Math.max(current, target); setProgress(splash, current); } bumpProgress(22); document.addEventListener( "andes:catalog-sync-progress", function (ev) { var detail = (ev && ev.detail) || {}; var total = Number(detail.total || 0); var done = Number(detail.done || 0); if (total > 0) { bumpProgress(28 + Math.round((done / total) * 52)); } else { bumpProgress(42); } }, { passive: true } ); document.addEventListener( "andes:catalog-sync-done", function () { bumpProgress(88); }, { once: true, passive: true } ); function finish() { if (finished) return; finished = true; bumpProgress(100); var elapsed = Date.now() - started; var wait = Math.max(0, SPLASH_MIN_MS - elapsed); window.setTimeout(function () { hideSplash(splash); }, wait); } if (document.readyState === "complete") { bumpProgress(72); finish(); } else { window.addEventListener( "load", function () { bumpProgress(72); finish(); }, { once: true } ); } window.setTimeout(function () { if (!finished) finish(); forceUnlockUi(document.getElementById("mobile-splash")); }, SPLASH_HARD_MAX_MS); document.addEventListener( "pointerdown", function () { forceUnlockUi(document.getElementById("mobile-splash")); }, { once: true, capture: true, passive: true } ); }); })();